node_modules/
.vercel/
.DS_Store

# Claves y configuracion generada: nunca al repo.
.env
.env.local
config.js
VAPID-NUEVAS.txt

# Respaldos de la base: traen montos, nombres del equipo y horarios reales.
# Y este repo se sirve tal cual en Vercel, asi que un JSON suelto aca queda
# publico en la web. Van fuera del proyecto.
respaldo-*.json

# Permisos locales de Claude Code: son de esta maquina, no del proyecto.
.claude/settings.local.json
